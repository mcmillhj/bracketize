{
  "rules": {
    ".write": "auth !== null",
    "users": {
			"$uid": {
      	"brackets": {
          ".read": true,
          ".write": "auth !== null && auth.uid === $uid"
        }
      }
    }
  }
}
