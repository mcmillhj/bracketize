{
  "rules": {
    ".write": "auth !== null",
    "brackets": {
      ".read": true,
      ".write": false
    },
    "users": {
      "$uid": {
        "brackets": {
          ".read": true,
          ".write": "auth !== null && auth.uid === $uid"
        }
      }
    }
  }
}
